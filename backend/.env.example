# Server
PORT=8000

# Ollama (LLM) - keep using your existing local Ollama install
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama2

# Embeddings - runs locally via Transformers.js, no API key needed
EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2

# Storage paths
VECTORSTORE_DIR=./vectorstore
